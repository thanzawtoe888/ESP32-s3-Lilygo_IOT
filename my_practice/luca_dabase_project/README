# From command line : cmd : type pyton
# import sqlite3
# >>> conn = sqlite3.connect("luca.db")
# >>> c = conn.cursor()
# >>> rows = c.execute("SELECT * FROM luca ORDER BY timestamp DESC LIMIT 1")
# >>> for row in rows:
# ...     print(row)